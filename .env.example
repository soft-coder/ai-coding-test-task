# Copy to `.env` and fill in local values. `.env` is gitignored.

# Backend API base URL. Consumed at build time to generate
# src/environments/environment.ts (see scripts/generate-environment.mjs).
API_BASE_URL=

# OpenAPI spec URL for `npm run gen:api` (Dockerized openapi-generator-cli).
SWAGGER_URL=
